# Byte Bot – Webhook Base

Servidor mínimo para pruebas con Freshchat.

Endpoints:
- GET / → verifica que el servidor está vivo
- POST /webhook → recibe mensajes de Freshchat

Para producción, integrar luego con OpenAI.
